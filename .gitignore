# VOS TOOL - Security & Privacy Protection

# ────────────── SENSITIVE FILES (NEVER COMMIT) ──────────────
# Environment files with credentials
.env
.env.local
.env.production
.env.staging
.env.development

# Backup files that may contain credentials
config_backup.py
.env_backup

*.tar
models/*.gguf
models/.cache/

# Dashboard data (contains user information)
# Ignore JSON files but allow dashboard_data structure
dashboard_data/**
!dashboard_data/.gitkeep
!dashboard_data/**/

# Audio recordings (privacy protection)
# Ignore all files in Recordings folder, but keep the folder structure
Recordings/*
!Recordings/.gitkeep
recordings/*
!recordings/.gitkeep
*.mp3
*.wav
*.m4a
*.flac

# Chrome profile sessions (may contain login data)
# Ignore all files in chrome_profile_sessions folder, but keep the folder structure
chrome_profile_sessions/*
!chrome_profile_sessions/.gitkeep
chromedriver.exe

# Backup directories
*_backup*/
dashboard_data_backup_*/

# Large external dependencies (not needed in repo)
llama.cpp/

# ────────────── PYTHON & DEVELOPMENT ──────────────
# Python cache
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
# lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
env/
ENV/
.venv/

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# ────────────── LOGS & TEMPORARY FILES ──────────────
# Log files
*.log
logs/
log/

# Temporary files
*.tmp
*.temp
temp/
tmp/

# Test files
test_*.py
debug_*.py
*_test.py

# ────────────── SYSTEM FILES & DIRECTORIES ──────────────
# Windows system directories (should not be in project)
AppData/
Application Data/
Cookies/
Local Settings/
My Documents/
NetHood/
PrintHood/
Recent/
SendTo/
Start Menu/
Templates/
Desktop/
Documents/
Downloads/
Pictures/
Music/
Contacts/
Favorites/
Links/
OneDrive/
# Only ignore root-level Models/ directory, not backend/models/
/Models/
App/
NTUSER.DAT*
*.regtrans-ms
*.TM.blf
*.TMContainer*

# IDE and editor cache
.windsurf/
.cursor/
.codeium/
.cache/
.AnyLogicPLE/
.streamlit/

# SSH keys and personal configs
.ssh/
.gitconfig
.bash_history
.python_history

# Temporary files
.tmp-*
.wdm/

# ────────────── SECURITY NOTES ──────────────
# This .gitignore protects:
# 1. Environment variables with credentials
# 2. User data and recordings (privacy)
# 3. Session data and temporary files
# 4. Development and debug files
# 5. System files and directories
#
# Always verify before committing:
# git status --ignored
# git add -n . (dry run to see what would be added)
