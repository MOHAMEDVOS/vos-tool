# ReadyMode Downloader Configuration
# Copy this file to .env and fill in your actual values
# IMPORTANT: Add .env to .gitignore to avoid committing credentials!

# ============================================================================
# REQUIRED: ReadyMode Credentials
# ============================================================================
READYMODE_USER=your_username_here
READYMODE_PASSWORD=your_password_here

# ============================================================================
# OPTIONAL: Paths
# ============================================================================
# Path to ChromeDriver executable (defaults to ./chromedriver.exe)
CHROMEDRIVER_PATH=chromedriver.exe

# Base directory for downloaded recordings (defaults to ./Recordings)
RECORDINGS_DIR=Recordings

# Chrome profile directory (defaults to ./chrome_profile)
CHROME_PROFILE_DIR=chrome_profile

# ============================================================================
# OPTIONAL: Timeouts (in seconds)
# ============================================================================
# Page load timeout (default: 60)
# PAGE_LOAD_TIMEOUT=60

# Element wait timeout (default: 30)
# ELEMENT_WAIT_TIMEOUT=30

# Download timeout (default: 120)
# DOWNLOAD_TIMEOUT=120

# ============================================================================
# OPTIONAL: Retry Settings
# ============================================================================
# Maximum download retries (default: 3)
# MAX_DOWNLOAD_RETRIES=3

# Delay between retries in seconds (default: 2)
# RETRY_DELAY=2

# ============================================================================
# OPTIONAL: Limits
# ============================================================================
# Maximum duration for "all users" downloads in minutes (default: 30)
# MAX_ALL_USERS_DURATION_MINUTES=30

# Maximum duration for single agent downloads in minutes (default: 15)
# MAX_SINGLE_AGENT_DURATION_MINUTES=15

# ============================================================================
# USAGE INSTRUCTIONS
# ============================================================================
# 
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Edit .env with your actual credentials
#
# 3. Add .env to .gitignore:
#    echo ".env" >> .gitignore
#
# 4. Install python-dotenv:
#    pip install python-dotenv
#
# 5. Load in your script:
#    from dotenv import load_dotenv
#    load_dotenv()
#
# ============================================================================
