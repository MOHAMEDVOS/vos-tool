# Environment Variables Template for VOS Tool
# This file is for reference only. Railway uses the Variables tab in the dashboard.
# Do not commit actual secrets to this file.

# Database Connection
DB_TYPE=postgresql
# Railway recommended: use DATABASE_URL (or the PG* variables Railway injects)
DATABASE_URL=
POSTGRES_HOST=
POSTGRES_PORT=
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_SSLMODE=require

# Security Keys (Generate new ones for production!)
SECRET_KEY=your_secret_key_here
JWT_SECRET=your_jwt_secret_here

# Application Settings
DEBUG=false
FRONTEND_URL=https://your-frontend-service.railway.app
CORS_ORIGINS=https://your-frontend-service.railway.app
BACKEND_URL=https://your-backend-service.railway.app

# Database Pool Settings (Optional)
DB_POOL_MAX_SIZE=5
DB_CONNECT_TIMEOUT=10
DB_QUERY_TIMEOUT=30000

# Redis (Optional)
REDIS_URL=redis://redis:6379

# Timezone
TZ=America/New_York

# AssemblyAI (Optional)
ASSEMBLYAI_API_KEY=your_assemblyai_api_key_here
