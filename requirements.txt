# VOS Tool - Production Requirements (CPU-Only)
# Last Updated: 2024
# Validated against actual codebase imports
# CPU-only deployment (no GPU support)

# ==================== Backend Dependencies ====================
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-multipart>=0.0.6
PyJWT>=2.8.0
python-dotenv>=1.0.0

# ==================== Frontend Dependencies ====================
streamlit>=1.28.0
streamlit-autorefresh>=1.0.0

# ==================== Data Processing ====================
pandas>=1.5.0
numpy>=1.21.0
scipy>=1.9.0

# ==================== Audio Processing ====================
librosa>=0.9.0  # Required for advanced audio processing (may be used by pydub)
pydub>=0.25.0   # Core audio manipulation library

# ==================== HTTP and API Clients ====================
requests>=2.28.0

# ==================== Machine Learning (Rebuttal Detection) ====================
sentence-transformers>=2.2.0  # Semantic matching for rebuttal detection (CPU-only, installs torch automatically)
scikit-learn>=1.3.0           # Cosine similarity calculations
transformers>=4.36.0          # Transitive dependency of sentence-transformers
accelerate>=0.24.0            # Model optimization (CPU support)
# torch>=2.0.0                # NOT INCLUDED: CPU-only deployment (sentence-transformers will install CPU torch automatically)

# ==================== String Matching ====================
thefuzz>=0.19.0  # Fuzzy string matching (used in audio_pipeline/detections.py)

# ==================== Transcription Service ====================
assemblyai>=0.48.0  # Cloud-based transcription (replaces local Whisper/Vosk)

# ==================== Automation (ReadyMode Integration) ====================
selenium>=4.15.0
webdriver-manager>=4.0.0

# ==================== Security ====================
cryptography>=41.0.0  # Encryption for sensitive data (lib/security_utils.py)
bleach>=6.0.0         # HTML sanitization (lib/html_sanitizer.py)

# ==================== Utilities ====================
psutil>=5.9.0  # System monitoring and CPU optimization

# ==================== Database ====================
psycopg2-binary>=2.9.0  # PostgreSQL adapter (cross-platform binary)

# ==================== Removed Dependencies (Not Used) ====================
# vosk>=0.3.45              # REMOVED: Replaced by AssemblyAI
# openai-whisper>=20231117  # REMOVED: Replaced by AssemblyAI (saves ~5-10GB)
# jellyfish>=1.2.0          # REMOVED: Not found in active codebase

# ==================== Optional Dependencies (Not Included) ====================
# torch>=2.0.0              # Optional: Only needed for GPU support (CPU version auto-installed by sentence-transformers)
# llama-cpp-python>=0.2.0   # Optional: Only used in experimental/archive code
# bitsandbytes>=0.41.0      # Optional: GPU quantization (not currently used)
# flash-attn>=2.0.0         # Optional: Flash Attention 2 (requires CUDA compilation)
