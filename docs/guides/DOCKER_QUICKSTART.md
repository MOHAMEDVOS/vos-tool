# Docker Quickstart

```bash
docker compose up --build
```

Open:

- Web app: `http://localhost:3000`
- API: `http://localhost:8000`
- API docs: `http://localhost:8000/docs`

The old Streamlit frontend is retired.
