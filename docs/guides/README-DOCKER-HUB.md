# Docker Hub Guide

Publish two runtime images:

- Backend: build with `backend/Dockerfile`, expose `8000`
- Webapp: build with `webapp/Dockerfile`, expose `3000`

The webapp replaces the old Streamlit image.
