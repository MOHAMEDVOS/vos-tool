# Railway Docker Config

Use Dockerfile-based Railway services:

- Backend Dockerfile path: `backend/Dockerfile`
- Webapp Dockerfile path: `webapp/Dockerfile`

Do not configure the retired Streamlit frontend.
