# Frontend Requirements for VOS Tool
# Includes Streamlit UI and ReadyMode automation dependencies

# ==================== Frontend-Specific Dependencies ====================
streamlit>=1.28.0
streamlit-autorefresh>=1.0.0

# Automation (ReadyMode integration - Chrome/Selenium)
selenium>=4.15.0
webdriver-manager>=4.0.0

# ==================== Shared Dependencies ====================
# Core data processing
pandas>=1.5.0
numpy>=1.21.0
scipy>=1.9.0

# Audio processing (required for audio pipeline)
librosa>=0.9.0
pydub>=0.25.0

# HTTP and API clients
requests>=2.28.0

# Environment and configuration
python-dotenv>=1.0.0

# Machine Learning (Rebuttal detection) - CPU-only PyTorch
# Note: PyTorch CPU-only will be installed separately in Dockerfile to avoid GPU dependencies (saves ~1-2GB)
# sentence-transformers will use the pre-installed PyTorch
sentence-transformers>=2.2.0
scikit-learn>=1.3.0
transformers>=4.36.0
accelerate>=0.24.0

# String matching and similarity
thefuzz>=0.19.0

# Transcription service (cloud-based, no local models)
assemblyai>=0.48.0

# Security and utilities
cryptography>=41.0.0
bleach>=6.0.0
psutil>=5.9.0

# Database
psycopg2-binary>=2.9.0

